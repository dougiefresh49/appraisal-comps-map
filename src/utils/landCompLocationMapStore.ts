import {
  type LocationMapState,
  SUBJECT_DEFAULT,
  LOCATION_DEFAULT,
  normalizeLocationState,
} from "./projectStore";

export const LandCompLocationMapStateUrl = "landCompLocationMapState";

export { normalizeLocationState, SUBJECT_DEFAULT, LOCATION_DEFAULT };
export type { LocationMapState };
